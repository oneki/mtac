INSERT INTO "field" (
  label,
  resource_type,
  "owner",
  peer_id,
  multiple,
  "required",
  validators,
  default_value,
  priv,
  "description",
  editable,
  example
)
VALUES(
  :label,
  :resourceType,
  :owner,
  :peerId,
  :multiple,
  :required,
  to_json(:validators::JSON),
  to_json(:defaultValue::JSON),
  :priv,
  :description,
  :editable,
  to_json(:example::JSON)
);