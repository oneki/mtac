INSERT INTO "resource" (
  label,
  pub,
  link_id,
  link_type,
  tenant_id,
  schema_id,
  created_by,
  content
)
VALUES(
  :label,
  :pub,
  :linkId,
  :linkType,
  :tenantId,
  :schemaId,
  :createdBy,
  to_json('{}'::JSON)
);